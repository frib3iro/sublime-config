[
    { "keys": ["f5"], "command": "build" },

    { "keys": ["f10"], "command": "exec",
      "args": { "cmd": ["lua", "$file"] }
    },
    {
        "keys": ["ctrl+shift+j"],
        "command": "pretty_json",
        "context": [
            { "key": "selector", "operator": "equal", "operand": "source.json" }
        ]
    }
]
